///component_shift_to(x,y)
/*
 * Moves a component to given position.
 * If it's a container, all items are moved to keep
   their relative position (which is enforced by
   component_shift_by script).
 */

component_shift_by(argument0-x, argument1-y);
